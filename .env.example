# Azure Application Insights connection string.
# Copy this file to .env.local and fill in the value from the Azure portal
# (Application Insights resource → Overview → Connection String).
# Leave blank (or omit the file entirely) to disable telemetry during local development.
VITE_APPINSIGHTS_CONNECTION_STRING=
